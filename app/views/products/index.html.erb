<h1>Super Powers</h1>
<h3>Get 'Em While They're Hot!</h3>

<%= link_to "Sort by Most Reviews", products_path(sort_by: "most_reviews")%>

<ul>
  <% @products.each do |product| %>
    <li>
      <%= link_to product.name, product_path(product) %> |
      Reviews: <%= product.reviews.length %> |
      Country of Origin: <%= product.country %>
    </li>
  <% end %>
</ul>

<%= link_to "New Super Power", new_product_path %>
